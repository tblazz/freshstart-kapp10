== README

This app uses Google Cloud Vision to detect text on photos.
Please make sure that you configured the environment variables accordingly. See file `.env.example.yml` as well as the [Google Cloud Vision documentation](https://cloud.google.com/vision/docs/) to set a project name ang get the credentials file
